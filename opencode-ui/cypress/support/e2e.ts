// E2E runs against the real opencode console. The vite-plugin-opencode dev
// fallback spawns it on the first /opencode-resolve hit, so no manual setup is
// needed beyond `npm run dev` (the test:e2e script handles both).
import './commands'
