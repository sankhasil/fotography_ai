# Production — remote console, local npm opencode-ai server as fallback.
# TODO: replace placeholder host with the real production console URL.
VITE_OPENCODE_URLS=https://opencode-console.example.com,http://localhost:4096
