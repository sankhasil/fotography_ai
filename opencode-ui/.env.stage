# Stage — remote console first, local npm opencode-ai server as fallback.
# TODO: replace placeholder host with the real stage console URL.
VITE_OPENCODE_URLS=https://opencode-console.stage.example.com,http://localhost:4096
